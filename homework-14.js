import { Modal } from "./Modal.js"

const modalVariableReg = new Modal('#modalWindow', 'registration-button', true)

const modalVariableAuth = new Modal('#authModal', 'auth-button', false)


/* setInterval(() => {
  console.log(modalVariableReg.isOpen())
}, 3000)

setInterval(() => {
  console.log(modalVariableAuth.isOpen())
}, 3000) */

